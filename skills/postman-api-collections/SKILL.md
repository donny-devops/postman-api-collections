---
name: postman-api-collections
description: Expert AI agent skill for Postman collections, Newman regression pipelines, API contract testing, and OWASP security audit assertions.
---

# Postman API Collections Skill

## Usage
- Run collection validations with `python scripts/run_newman_tests.py`.
- Run automated Newman CI tests via `newman run <collection.json> -e <environment.json>`.
- Audit REST and GraphQL APIs for standard security headers (`Strict-Transport-Security`, `X-Content-Type-Options`, `X-Frame-Options`).
